<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="NewsRss">

    <!-- 뉴스 RSS 목록 조회 -->
    <select id="selectNewsRssList" resultType="hashMap">
		select rss_seq
             , url
             , use_yn
		  from tbl_news_rss a
		      inner join (
			      select rss_seq 
			        from tbl_news_rss 
			       limit #start#, #pageCount#) b
		using(rss_seq)
    </select>

</mapper>